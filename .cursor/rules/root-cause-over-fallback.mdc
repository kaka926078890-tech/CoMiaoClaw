---
description: 出错时追究根因，不无限制做兜底与兼容
globs:
alwaysApply: true
---

# 追究根因，不做无限制兜底

- 当出现**不符合约定/协议**的行为或输出（例如协议要求 `DELEGATE:` 却得到 `DELEG:`）时，**优先追究错误原因**：检查人设（AGENTS.md/persona）、示例、模型行为、调用方式等，从源头修正。
- **禁止**在未弄清原因的情况下，通过「再兼容一种写法」「再加一层 fallback」等方式无限制放宽协议或增加兜底逻辑；此类修改会掩盖问题并让协议边界模糊。
- 仅在根因明确且确有需求时（例如文档约定接受多种合法格式），才扩展解析或兼容；扩展时需同步更新文档与规则，保持契约清晰。
