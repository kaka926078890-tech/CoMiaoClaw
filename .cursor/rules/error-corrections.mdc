---
description: 错误纠正记录 — 记录用户纠正 AI 的错误，避免重复犯错
alwaysApply: true
---

# 错误纠正记录

> 本文档记录每次用户纠正 AI 的错误，避免重复犯错。纠正后须同步更新对应规则文件。

---

## 记录格式

每次纠正后，按以下格式添加：

```markdown
### YYYY-MM-DD - [错误类型] 简要描述

**错误场景**：描述当时的情况
**错误行为**：AI 做了什么错误的事
**正确做法**：应该如何做
**规则更新**：已添加到哪个规则文件的哪一条
```

---

## 纠正记录

### 2025-03-02 - [错误处理策略] 出现错误格式时做兜底兼容而非追究根因

**错误场景**：主 agent 返回了 `DELEG:` 而非协议规定的 `DELEGATE:`，导致只解析到一条派发、只执行一个子 agent。

**错误行为**：在未弄清模型为何输出错误格式的情况下，修改网关解析逻辑，增加对 `DELEG:` 的兼容，并更新文档称「两种写法均有效」。

**正确做法**：保持协议只认标准格式 `DELEGATE:`；应追究根因（人设/示例/模型行为等）并从源头修正。不通过无限制增加兼容写法来掩盖问题。

**规则更新**：已新增 `.cursor/rules/root-cause-over-fallback.mdc`，约定出错时优先追究根因、禁止无限制兜底与兼容。

### 2025-03-03 - [兜底] TIME_TASK 从思考内容解析

**错误场景**：模型把 `TIME_TASK:` 行写在思考过程里，主回复正文未包含该行，导致网关解析不到、定时任务未创建。

**错误行为**：在网关流式逻辑中增加「从 fullThinking 兜底解析」：若主回复解析不到再从思考内容里解析 TIME_TASK。

**正确做法**：不做兜底。数据从哪里来就从哪里取——系统只从主回复正文（fullReply）解析；若解析不到，应追究根因：通过人设/提示词明确要求「TIME_TASK 行必须写在主回复正文中，禁止只写在思考里」，从源头约束模型输出位置。

**规则更新**：已撤销网关对 fullThinking 的兜底解析；已更新 `gateway/data/AGENTS.md` 创建定时任务一节，明确「系统仅从主回复正文解析」「禁止把 TIME_TASK 写在思考里」。

---

## 使用说明

- 每次用户纠正后，立即在此文件添加一条记录。
- 同时更新对应的规则文件（如 project.mdc、git-commit.mdc 等），确保规则可执行。
- 定期回顾此文件，避免重复犯错。
