---
description: Git 提交规范 — 粒度、message 格式、提交前自检
globs: "**/*.{ts,tsx,js,jsx,json,md,css,html}"
alwaysApply: true
---

# Git 提交规范

执行 git 相关操作（提交、建议 commit message、代码评审前）时须遵循以下约定。

## Commit 粒度

- 按**可独立回滚、语义清晰**的单元提交：一个 commit 只做一类改动。
- 避免一个大 commit 混合多个不相关修改；功能开发可拆为多个 commit（如先 UI 骨架、再接接口、再补错误处理）。

## Commit Message 格式

- **格式**：`<type>: <简短描述>`；必要时在正文补充说明。
- **type** 使用：
  - `feat`: 新功能
  - `fix`: 修复 bug
  - `refactor`: 重构（不改变行为）
  - `style`: 样式/格式（不含逻辑）
  - `docs`: 文档
  - `chore`: 构建/工具/配置/脚本等
- **描述**：简明说明「做了什么」，使用中文或英文均可，同一项目内保持一致；不写实现细节。

## 提交前自检

- 不提交调试用代码（如 `console.log`、临时 mock 开关）、注释掉的大段代码或与本次改动无关的格式整理。
- 若改动影响现有功能，需保证相关流程可运行、无明显报错后再提交。
- 确认未提交敏感信息（如 `.env`、密钥）；依赖 `.gitignore` 排除无需入库的文件。

## 分支与协作

- 功能开发建议在单独分支进行，按团队约定提 MR/PR；合并前确保描述可引用对应文档或需求结论。
