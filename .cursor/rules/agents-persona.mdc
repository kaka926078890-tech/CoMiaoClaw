---
description: 修改 AGENTS/人设时避免限定、硬编码与写死内容
globs: gateway/data/AGENTS.md,**/AGENTS.md,**/persona*.md
alwaysApply: false
---

# 人设与 AGENTS 修改原则

修改 `AGENTS.md` 或其他人设/角色文档时：

- **避免硬编码**：不要针对单一技能、单一子角色名或单一场景写死规则（例如「访问网页必须用 SKILL: browser」）。改为可复用的通用表述（例如「只能派发给当前列出的子角色」）。
- **避免过度限定**：用「仅当…」「只可…」等时，尽量基于**系统实际提供的信息**（如「当前子角色及其能力」列表、「可用技能」列表），而不是枚举具体名称或行为。
- **避免写死内容**：新增说明时优先写**原则**（如「禁止向不存在的子角色派发」），不优先写**具体案例**（如「不要派发给 browser」）；若必须举例，注明为示例并保持可扩展。
- **保持可扩展**：新增技能或子角色后，人设应无需改就能生效，而不是在人设里再补一条「某场景用某技能/某角色」。
