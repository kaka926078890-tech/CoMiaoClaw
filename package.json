{
  "name": "comiao-claw",
  "private": true,
  "version": "0.0.1",
  "description": "Claw：V1 现有实现 / V2 重写版",
  "scripts": {
    "dev": "npm run dev --prefix v1",
    "dev:v1": "npm run dev --prefix v1",
    "start": "npm run dev",
    "install:all": "npm install --prefix v1 && npm run install:all --prefix v1"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
