{
  "name": "comiao-claw",
  "private": true,
  "version": "0.0.1",
  "description": "Claw 对话控制台：网关 + 前端",
  "scripts": {
    "dev": "concurrently -n gw,fe -c blue,green \"npm run dev --prefix gateway\" \"npm run dev --prefix frontend\"",
    "start": "npm run dev",
    "install:all": "npm install --prefix gateway && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
