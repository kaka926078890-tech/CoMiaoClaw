# 对话总结 · 2026-03-01（6）

## 本对话涉及的需求与结论

### 主·子 Agent（DELEGATE）与前端展示

- **综合回复**：应为对子 agent 内容的**总结与归纳**，不应再次输出 DELEGATE 派发任务。已在网关综合阶段 user 消息前增加强约束文案；若模型仍输出 DELEGATE，可再加强 persona 或单独 system 片段。
- **无效内容不展示**：主回复中的 DELEGATE 行（红框）不在前端展示。有 subAgents 或 summary 时只展示 mainReplyClean，为空则展示「已派发子任务。」，不 fallback 到 content。
- **子 agent 结论外置**：每个子 agent 的「思考过程」可折叠、「结论」独立一块在思考过程外边、不折叠，与思考过程明确分离。
- **OpenClaw 参考**：总结应按 **rules** 在 `memory/` 下新建 `MMDD-N.md`，而非仅回复或只写 docs。本次已按 `.cursor/rules/project.mdc`「总结命令与记忆文件夹」执行。

### 已处理内容（本轮及此前）

- P0 主/子 agent 协同、主/子共用同一模型、网关 main_reply_clean/summary/sub_* 结构化事件、前端主回复与综合回复分离、Markdown 渲染、复制/重发、子 agent 结论外置、综合阶段强约束、红框 DELEGATE 不展示。
- 文档：`docs/delegate-status-and-todo.md`（当前问题与待办）、`docs/sub-agent-flow.md`（流程与 OpenClaw 参考）。

### 待办 / 后续

- 若综合回复仍出现 DELEGATE：加强综合轮 system 或 persona。
- 子 agent 流式观感：可考虑接收流时默认展开对应子 agent 块。
- 子 agent 并行（无依赖时）：见 `docs/openclaw-delegate-questions.md`。

---

## 当前项目状态（与本对话相关）

- **网关**：DELEGATE 解析、串行子 agent（含流式 sub_thinking/sub_chunk）、综合阶段强约束、结构化 SSE 事件。
- **前端**：mainReplyClean/summary 展示、子 agent 思考可折叠/结论外置、无意义 DELEGATE 不展示。
- **规则**：用户发「总结」时须在 `memory/` 新建 `MMDD-N.md`（命名 `MMDD-N`，N 全局递增）。

---

## 快速恢复上下文（新对话时）

1. 看 **docs/delegate-status-and-todo.md** 了解主/子 agent 当前状态与待办。
2. 看 **memory/** 下最新 `MMDD-N.md` 了解近期结论与规则约定。
3. 前端在 **frontend/**，网关在 **gateway/**；规则在 **.cursor/rules/**，总结须写 **memory/MMDD-N.md**。
